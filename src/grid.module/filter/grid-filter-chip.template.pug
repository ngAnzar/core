:stylus
  .close-button
    margin-left: 8px
    margin-right: -2px

  .field-title
    color: #888

  .filter-value
    margin-left: 4px


span.field-title {{ title }}{{ titleSeparator }}
div.filter-value
  ng-content

.nz-fab.close-button(on-tap="onRemoveClick($event)")
  nz-icon(class=icon("mdi/close", 20))
