
form(#container, fxFlex="grow", fxLayout="row", fxLayoutAlign="start center", fxLayoutGap="8px", ngNoForm, (submit)="doSearch();$event.preventDefault()")
  i.nz-icon(fxShow, fxHide.xs, [class]="icon")

  //- .nz-button(fxFlex="0 0 28px", fxHide, fxShow.xs, (tap)="$event.preventDefault();opened=false", variant="tiny filled icon")
  //-   i.nz-icon(class=icon("mdi/arrow-left"))

  .nz-placeholder(fxFlex="grow", variant="hide")
    label {{ label }}
    input.nz-input([formControl]="control")

  .nz-button(*ngIf="!empty", fxFlex="0 0 28px", variant="tiny filled icon", (tap)="doReset()")
    i.nz-icon(class=icon("mdi/close"))


//- ng-template(nzViewportItem="topRight:-100")
//-   button.nz-button(variant="medium icon", fxHide, [fxShow.xs]="!vps.navbarCenterOverlap", (tap)="opened=true")
//-     i.nz-icon([class]="icon")
