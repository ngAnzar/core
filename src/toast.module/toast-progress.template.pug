:stylus
  .spinner
    padding: 8px

  .text
    visibility: none

.nz-hbox(align="center")
  div.spinner
    nz-progress(type="circle" radius="20" bind-source="options.progress" bind-indeterminate="options.indeterminate")

  //- div.text(#success bind-innerHTML="options.success")
  //- div.text(#failure bind-innerHTML="options.failure")
