export const Zone = (window as any).Zone

export const __zone_symbol__ =
    (typeof Zone !== "undefined") && (Zone as any)["__symbol__"] || function (v: string): string {
        return "__zone_symbol__" + v
    }
