:stylus
  .scrollable
    //transition: transform 10ms
    //- min-width: 100%
    //- min-height: 100%
    display: grid


//- div {{ lastEvent.type }} -- {{ lastEvent.additionalEvent }}

div.scrollable(#scrollable)
  ng-content

.nz-scrollbar(orient="horizontal")
.nz-scrollbar(orient="vertical")
